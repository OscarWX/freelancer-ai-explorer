# Copy this file to .env.local and fill in your own key.
# You ONLY need a key to regenerate summaries with build_app_data.py.
# Just viewing index.html needs no key.
OPENAI_API_KEY=sk-your-key-here
# Optional: override the model (otherwise the newest available is auto-picked).
OPENAI_MODEL=gpt-4o-mini
